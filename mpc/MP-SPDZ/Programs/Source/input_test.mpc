program.use_trunc_pr = True

sfix.set_precision(8,16)
demo_input_test = sfix.Tensor([1, 2])
# demo_input_test.read_from_file(0, 187)[1]
start = 0
for element in demo_input_test:
    start = element.read_from_file(start)
demo_input_test.print_reveal_nested()

expected = sfix.Tensor([1,2])
w0_dim0 = sint.get_input_from(0)
w0_dim1 = sint.get_input_from(0)
expected.input_from(0)
expected.print_reveal_nested()

@for_range(len(expected[0]))
def _(i):
    sfix.write_to_file(expected[0][i])
# d = a + b + c
# print_ln('a = %s, b = %s, c = %s, d = %s', a.reveal(), b.reveal(), c.reveal(), d.reveal())